# Thread count is a real config axis on a 4P+4E machine: oversubscribing the
# efficiency cores costs more than it buys.
label: ort-cpu-4thread
runtime:
  kind: onnxruntime
  providers: [CPUExecutionProvider]
  graph_optimization_level: all
execution:
  num_threads: 4
